<!DOCTYPE html>
<html>
  <head>
    <title>GoldenOwlDemo</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'https://kit.fontawesome.com/f3d6bce8e0.js', crossorigin:"anonymous" %>
</head>

  <body>
    <div class="wrapper">
      <% unless user_signed_in? && current_user.has_role?(:admin) %>
        <%= render "layouts/navigation" %> 
      <% else %>
        <%= render "admin/navigation" %>
      <% end %>
      <%= yield%>
      <%= render "admin/footer" if user_signed_in? && current_user.has_role?(:admin) %>
    </div>
  </body>
</html>
